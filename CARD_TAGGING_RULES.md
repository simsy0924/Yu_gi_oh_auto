# 카드 태그 분류 규칙

## 1. 저장소 기준

이 규칙은 `app/page.tsx`의 `EffectActivationTag`,
`EffectApplicationTag`, `EffectOperationTag` 정의를 기준으로 한다.
분류기는 아래에 열거된 기존 태그만 사용할 수 있다. 새 문자열 태그,
번역한 태그, 카드군 전용 태그를 만들면 안 된다.

카탈로그 원본은 `public/data/ygo-ko-cards/manifest.json`과
`part-*.json`이다. 주요 필드는 다음과 같다.

- `i`: 카탈로그 내부 숫자 ID
- `p`: 카드 패스코드 또는 `null`
- `n`: 카드 이름
- `t`: 몬스터 본문/마법·함정 효과 텍스트
- `e`: 펜듈럼 효과 텍스트
- `c`, `r`, `y`: 카드 종류, 마법·함정 세부 종류, 몬스터 분류
- `g`: 링크 마커이며 태그 배열이 아니다.
- `u`: 미발매 번역 표시이며 태그가 아니다.

`i`는 실제 데이터에서 중복되므로 파이프라인의 카드 ID는
`i:p` 형식의 복합 키를 사용한다. `p`가 없으면 `i:null`을 사용한다.

원본 카탈로그에는 앱의 효과 태그가 저장되어 있지 않다. 병합 결과는
원본을 수정하지 않고 `.codex-tagging/tagged_cards.json` 사이드카에만
저장한다.

## 2. 최우선 판정 원칙

1. 카드 이름이나 카드군의 일반적인 사용법으로 추정하지 않는다.
   오직 입력에 포함된 효과 텍스트, 공통 제한 문구, 카드 종류와 효과
   구조를 근거로 판정한다.
2. 입력의 카드와 `effect_id`를 추가, 삭제, 합치기, 재분할하지 않는다.
   번호가 없어서 여러 효과의 경계를 확정할 수 없으면 기존 효과 단위를
   유지하고 `needs_review=true`로 보낸다.
3. 한 효과가 여러 처리를 실제로 수행하면 해당하는 기존 태그를 모두
   붙인다. 선택지 효과는 어느 선택지를 골라도 발생 가능한 처리 태그의
   합집합을 붙이되, 선택지별 조건 차이가 중요하면 검토 대상으로 보낸다.
4. 코스트와 효과 처리를 구분한다. 처리 내용 태그는 원칙적으로 효과의
   해결 시 수행하는 처리에 붙인다. 다만 `DETACH_XYZ`는 저장소의 기존
   사용 방식상 엑시즈 소재 제거 코스트도 표시할 수 있다.
5. 단순 조건 언급과 실제 처리를 구분한다. 예를 들어 “파괴되었을 경우”
   는 `DESTROY`가 아니라 발동 조건이며, 효과가 다른 카드를 파괴할 때만
   `DESTROY`를 붙인다.
6. 텍스트가 비어 있거나 깨졌거나, 번역이 불완전하거나, 룰 텍스트와
   발동 효과의 경계가 불명확하면 추측하지 않고 `needs_review=true`로
   표시한다.
7. 효과 단위 신뢰도가 `0.75` 미만이면 그 효과와 카드 모두
   `needs_review=true`여야 한다.

## 3. 발동 조건 태그

| 태그 | 적용 조건 | 제외 조건 |
| --- | --- | --- |
| `IGNITION` | 자신 메인 페이즈 등 공개 게임 상태에서 플레이어가 임의로 발동하는 기동 효과 | 유발 효과, 빠른 효과, 지속효과, 체인을 만들지 않는 룰 소환 |
| `QUICK` | “상대 턴에도”, “퀵 이펙트”, 유발즉시 효과처럼 스펠 스피드 2로 발동 가능 | 특정 사건 뒤 자동으로 발동하는 일반 유발 효과, 카운터 함정이라는 이유만으로 몬스터 효과에 부여 |
| `TRIGGER` | “~했을 경우/때 발동”처럼 사건을 계기로 발동 | 단순 적용 조건, 지속효과, 임의 기동 효과 |
| `ON_NORMAL_SUMMON` | 일반 소환 성공을 직접 발동 조건으로 함 | 일반 소환을 수행하는 효과(`NORMAL_SUMMON`) 또는 소환 시도 |
| `ON_SPECIAL_SUMMON` | 특수 소환 성공을 직접 발동 조건으로 함 | 특수 소환을 수행하는 효과(`SPECIAL_SUMMON`) 또는 소환 시도 |
| `ON_FLIP` | 리버스, 반전 소환, 앞면 표시가 된 사건에 직접 반응 | 단순히 뒷면/앞면 표시를 변경하는 처리 |
| `ON_ATTACK` | 공격 선언, 전투 실행, 전투 종료 등 전투 사건에 직접 반응 | 공격력 변경만 하는 효과, 전투 데미지만 조건인 효과 |
| `ON_DAMAGE` | 전투/효과 데미지 발생이나 데미지 계산을 직접 조건으로 함 | 단순 공격 선언 조건, 라이프 지불 코스트 |
| `ON_SENT_GY` | 카드가 묘지로 보내진 사건에 직접 반응 | 묘지에서 임의로 발동하는 효과, 묘지로 보내는 처리 |
| `ON_BANISHED` | 카드가 제외된 사건에 직접 반응 | 제외 상태에서 임의로 발동하는 효과, 제외 처리 |
| `ON_DETACH_XYZ` | 엑시즈 소재가 제거된 사건 자체에 반응 | 이 효과의 코스트로 소재를 제거할 뿐인 경우 |
| `ON_CARD_EFFECT` | 카드 효과의 발동에 직접 체인하는 조건 | 몬스터 효과에만 한정되면 `ON_MONSTER_EFFECT`; 단순 무효 처리 문구 |
| `ON_MONSTER_EFFECT` | 몬스터 효과의 발동에 직접 체인하는 조건 | 마법·함정을 포함하는 일반 카드 효과 조건 |
| `ON_SUMMON_ATTEMPT` | 소환 성공 전의 소환 시도/소환 자체를 무효로 하는 타이밍 | 소환 성공 후 발동하는 효과 |
| `CUSTOM` | 기존 태그로 표현할 수 없는 명시적 발동 조건 | 편의를 위한 포괄 태그로 사용 금지. 사용 시 이유를 적고 `needs_review=true` |

`TRIGGER`와 사건별 태그는 함께 사용할 수 있다. 예를 들어 “특수 소환에
성공했을 경우 발동”은 `TRIGGER`와 `ON_SPECIAL_SUMMON`을 모두 사용한다.
빠른 유발 효과라면 문구가 뒷받침하는 경우 `QUICK`도 함께 사용할 수 있다.

## 4. 적용 조건 태그

| 태그 | 적용 조건 | 제외 조건 |
| --- | --- | --- |
| `WHILE_FACE_UP` | 앞면 표시로 존재하는 동안 계속 적용 | 앞면 카드가 한 번 발동하는 효과 |
| `WHILE_IN_GY` | 묘지에 존재하는 상태가 발동/적용의 핵심 조건 | 묘지로 보내졌을 때만 반응하거나 묘지 카드를 대상으로 할 뿐인 효과 |
| `WHILE_BANISHED` | 제외되어 있는 상태가 발동/적용의 핵심 조건 | 제외되었을 때만 반응하거나 다른 카드를 제외하는 효과 |
| `WHILE_XYZ_MATERIAL` | 카드가 엑시즈 소재인 동안 적용되거나 부여됨 | 소재를 제거하거나 소재로 만드는 처리만 있는 효과 |
| `TARGET_REQUIRED` | 텍스트가 발동 시 대상을 지정함 | 해결 시 고르는 비대상 선택, 단순히 “그 카드”를 참조 |
| `NON_TARGETING` | 대상 지정 없이 해결 시 선택하거나 전체에 적용함이 명확 | 대상 여부를 확정할 수 없는 텍스트. 이때는 태그 없이 검토 |
| `SELF` | 자신, 자신의 카드/필드/라이프에 적용 | 자신이 발동한다는 사실만으로 부여하지 않음 |
| `OPPONENT` | 상대, 상대 카드/필드/라이프에 적용 | 상대 턴에 발동 가능하다는 사실만으로 부여하지 않음 |
| `MONSTER` | 몬스터를 적용 대상 또는 적용 범위로 삼음 | 몬스터 효과의 발동 조건만 언급 |
| `SPELL_TRAP` | 마법·함정 카드를 적용 대상 또는 범위로 삼음 | 마법·함정 존에서 발동한다는 사실만으로 부여하지 않음 |
| `EXTRA_DECK_MONSTER` | 엑스트라 덱 몬스터 또는 엑스트라 덱에서 특수 소환된 몬스터에 적용 | 엑스트라 덱에서 소환하는 처리 자체 |
| `CUSTOM` | 기존 적용 태그로 표현할 수 없는 명시적 범위/상태 | 불확실성을 숨기는 용도로 사용 금지. 사용 시 이유를 적고 `needs_review=true` |

`TARGET_REQUIRED`와 `NON_TARGETING`은 같은 효과 단위에 동시에 붙이지 않는다.
한 효과 안의 서로 다른 선택지가 대상/비대상으로 갈리는데 입력 구조가 이를
분리하지 못하면 둘 다 확정하지 말고 검토 대상으로 보낸다.

## 5. 처리 내용 태그

| 태그 | 적용 조건 | 제외 조건 |
| --- | --- | --- |
| `SEARCH` | 덱에서 카드를 패에 넣음 | 드로우, 묘지/제외에서 패로 회수 |
| `DRAW` | “드로우”라고 명시 | 덱에서 특정 카드를 패에 넣는 서치 |
| `SEND_DECK_TO_GY` | 덱에서 묘지로 보냄 | 패/필드/엑스트라 덱에서 묘지로 보냄 |
| `SPECIAL_SUMMON` | 효과 해결로 특수 소환함 | 소환 성공 시 반응, 체인을 만들지 않는 룰 특수 소환 조건 |
| `NORMAL_SUMMON` | 효과 해결로 일반 소환을 추가 수행함 | 통상적인 턴 1회 일반 소환, 일반 소환 성공 시 반응 |
| `DESTROY` | 효과 해결로 카드를 파괴함 | 파괴를 조건으로 삼거나 파괴 내성을 부여 |
| `BANISH` | 효과 해결로 카드를 제외함 | 제외 코스트만 존재, 제외 상태를 참조 |
| `SEND_GY` | 덱 이외의 장소에서 묘지로 보냄 | 파괴 결과로 묘지에 가는 것만 언급, 덱에서 묘지로 보냄 |
| `RETURN_HAND` | 필드/묘지/제외 등의 카드를 패로 되돌림 | 덱에서 패에 넣음, 패를 덱으로 되돌림 |
| `RETURN_DECK` | 카드를 덱/엑스트라 덱으로 되돌림 | 덱 위에 새 카드를 놓는 처리, 패로 되돌림 |
| `NEGATE_EFFECT` | 카드/몬스터의 효과를 무효로 함 | 발동 자체만 무효로 하고 효과 무효가 명시되지 않음 |
| `NEGATE_ACTIVATION` | 카드 또는 효과의 발동을 무효로 함 | 해결된 효과를 사후 무효화 |
| `CHANGE_ATK_DEF` | 공격력 또는 수비력을 올리거나 내리거나 특정 값으로 함 | 전투 데미지 변경, 원래 수치 단순 참조 |
| `CHANGE_LEVEL_RANK` | 레벨 또는 랭크를 변경함 | 레벨/랭크를 조건이나 소재 조건으로만 참조 |
| `CHANGE_TYPE_ATTRIBUTE` | 종족 또는 속성을 변경함 | 종족/속성을 조건으로만 참조 |
| `ATTACH_XYZ` | 카드를 엑시즈 소재로 겹침 | 엑시즈 소환의 통상 소재 사용 |
| `DETACH_XYZ` | 엑시즈 소재를 제거함 | 제거되었을 때 반응만 하는 효과 |
| `GRANT_EFFECT` | 다른 카드/몬스터가 효과를 얻거나 사용할 수 있게 함 | 수치·내성만 부여, 현재 카드가 자체 효과를 얻음이 불명확 |
| `TAKE_CONTROL` | 상대 카드의 컨트롤을 얻음 | 일시적으로 조작하거나 소유권만 언급 |
| `SUMMON_LOCK` | 플레이어의 일반/특수/특정 소환을 금지·제한함 | 소환 조건, 소재 제한, 단순 소환 실패 |
| `EFFECT_LOCK` | 효과 발동 또는 효과 사용을 금지·제한함 | 효과 무효 처리, 특정 카드의 공격 제한 |
| `CUSTOM` | 기존 처리 태그로 표현할 수 없는 실제 해결 처리 | “기타”를 남발하지 않는다. 사용 시 처리 내용을 note에 쓰고 `needs_review=true` |

파괴 후 제외, 되돌린 후 드로우처럼 한 효과가 순서대로 여러 처리를 하면
모든 해당 처리 태그를 붙인다. “A하고, 그 후 B”처럼 앞 처리가 실패하면
뒤 처리가 적용되지 않는 관계는 태그를 빼는 이유가 아니며, 필요하면 note에
의존 관계를 기록한다.

## 6. 복수 효과와 복수 태그

- `main`과 `pendulum`은 서로 다른 효과 영역으로 유지한다.
- 번호가 있는 효과는 입력의 번호별 `effect_id`에 독립적으로 태깅한다.
- 공통 카드명 횟수 제한 문구는 모든 효과의 태그로 복제하지 않는다.
  태그 판정에 필요한 조건으로만 참고한다.
- 한 번호 안에 여러 문장이 있어도 하나의 해결 과정이면 같은 효과에 여러
  태그를 붙인다.
- 한 번호 안에 독립 발동 가능한 효과가 섞여 있거나, 번호 없는 텍스트에
  독립 효과가 여러 개 이어져 있어 입력 구조만으로 분리할 수 없으면
  구조를 임의 변경하지 말고 `needs_review=true`로 보낸다.
- 효과가 없는 일반/토큰 몬스터는 `effects=[]`로 출력한다. 효과 여부가
  카드 분류와 텍스트만으로 확실하지 않으면 검토 대상으로 보낸다.

## 7. `needs_review` 기준

다음 중 하나라도 해당하면 효과와 카드의 `needs_review`를 `true`로 한다.

- 효과 텍스트가 없거나 깨져 의미를 확정할 수 없음
- 번호 없는 복수 효과의 경계가 불명확함
- 대상 지정 여부, 발동 영역, 발동/지속 효과 여부를 확정할 수 없음
- 코스트와 효과 처리를 구분할 수 없음
- 선택지별 태그가 크게 다르지만 입력 효과 단위가 분리되지 않음
- 기존 태그만으로 핵심 처리를 표현할 수 없어 `CUSTOM`을 사용함
- 카드명, 관행, 외부 룰링 없이는 결론을 낼 수 없음
- 미발매 번역(`unreleased_translation=true`)의 문구가 어색하거나 불완전함
- 효과 또는 카드 신뢰도가 `0.75` 미만임

불확실한 태그를 빈 배열로 숨기지 않는다. 확실한 태그만 기록하고,
불확실한 부분과 필요한 수동 확인 내용을 `note`에 짧고 구체적으로 적는다.
